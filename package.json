{
  "name": "hackerpro-migration",
  "version": "1.0.0",
  "description": "HackerPro domain migration notice",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "build": "echo \"No build required\" && exit 0",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=14.0.0"
  }
}
